"$schema": "http://json-schema.org/draft-04/schema#"
title: RemoveGroupMemberResponse
description: remove group member response
type: object
additionalProperties: false
properties:
    groupId:
        description: group id
        type: string
    members:
        description: group members not exist
        type: array
        items:
            type: string
required:
    - groupId

